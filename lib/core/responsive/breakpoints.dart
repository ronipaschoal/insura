/// Layout breakpoints shared across the app. Below [desktop], screens use a
/// mobile [Drawer]; at or above it, a fixed side menu is used instead.
abstract final class Breakpoints {
  static const double mobile = 600;

  static const double desktop = 1024;
}
